<h1 align="center">Welcome <%= @user.username %></h1>

<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
  <%= gravatar_for @user, size: 150 %>
  </div>
</div>

  <h4 align="center"><%= @user.username %>'s resources</h4>
    <div class="center">
      <%= will_paginate @user_resources %>
    </div>
<%= render 'resources/resource', obj: @user.resources %>
    <div class="center">
      <%= will_paginate @user_resources %>
    </div>
 
